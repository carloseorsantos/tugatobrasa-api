# Template do .env de produção — NUNCA commitar o .env real.
# Na instância Lightsail: copie para .env ao lado do docker-compose.yml
# e gere a senha de verdade com:
#   openssl rand -base64 24
POSTGRES_PASSWORD=changeme
